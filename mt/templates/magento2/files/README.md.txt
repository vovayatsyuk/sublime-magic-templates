# {project|camelcase}

${{0:{project|camelcase} - is a Magento2 module that}}

### Installation

```bash
cd <magento_root>
composer require {vendor}/{project}
bin/magento module:enable {vendor|camelcase}_{project|camelcase}
bin/magento setup:upgrade
```
